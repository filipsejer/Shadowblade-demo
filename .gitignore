# build output
out/
*.class

# run.sh logs / crash dumps
game.log
hs_err_pid*.log

# macOS
.DS_Store

# dev scratch files that have ended up in the project root (screenshots and audio samples
# taken while tuning graphics/sound, or by test tools' shot() helper writing beside cwd
# instead of the scratchpad when run standalone; not part of the game or the repo)
*.wav
[0-9]*_*.png
audio_lab_laugh.png
audio_sfx_contact_sheet.png
